#include "main.h"

/**
 * _strstr - a function that locates a substrings
 *
 * @haystack:
 * @needle:
 *
 * Return: Always(0)
 */

char *_strstr(char *haystack, char *needle)
{
	int i, j;

	for (i = 0; haystack[i] != NULL; i++)
	{
		for (j = 0; needle[i] != NULL; j++)
		{

		}
	}
}
