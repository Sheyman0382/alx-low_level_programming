#include "main.h"

/**
 * sub - a function that subtracts one integers from another
 * @a: the first integer of the function
 * @b: the second integer of the function
 *
 * Return - the subtraction of two integers
  */

int sub(int a, int b)
{
	return (a - b);
}
